/**
 * Site-wide identity & metadata.
 *
 * Sourced from data/portfolio.json (edited via the dev-only dashboard) and
 * re-exported here so components can keep importing from `@/constants/site`.
 */
import { data } from "@/lib/data";

export const site = data.site;
export const socials = data.socials;
export const nav = data.nav;
export const theme = data.theme;
