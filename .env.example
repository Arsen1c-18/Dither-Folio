# Passcode for the dev-only admin dashboard at /admin.
# Copy this file to .env.local and set a value. The dashboard is hidden in
# production, so this is only used during `npm run dev`.
ADMIN_PASSCODE=change-me
